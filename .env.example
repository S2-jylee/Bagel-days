# Supabase project — safe to expose in the browser (protected by Row Level
# Security policies on the `orders` table, not by keeping these secret).
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxx
